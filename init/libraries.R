library(puttytat4R)
outputSectionTitle("Loading libraries")

library(RPostgreSQL) 
library(dplyr)
library(ggplot2)
library(grid) # for legend.key.height = unit(...)
library(gridExtra) # for gtable_width
library(reshape2)

printDone()